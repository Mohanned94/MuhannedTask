<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muhanned_task">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_businessman"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_businessman"
        android:supportsRtl="true"
        android:name=".utilities.MyApplication"
        android:theme="@style/Theme.Muhanned_Task">
        <activity android:name=".Activities.jobdetailsActivity.JobDetailsActivity">

        </activity>
        <activity android:name=".Activities.homeActivity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>